#include <assert.h>
int main (void)
{
  int i, j, k[3];
  j = 0;
  for (i=0; i < 3; i++)
    {
      k[i] = j++;
    }

  for (i=2; i >= 0; i--)
      assert(k[i] == i);
  return 0;
}

#ifdef EiCTeStS
main();
#endif
